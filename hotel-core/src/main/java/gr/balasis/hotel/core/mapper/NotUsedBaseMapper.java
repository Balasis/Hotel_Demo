package gr.balasis.hotel.core.mapper;

public interface NotUsedBaseMapper<D,R,E>{
    // mapstruck enforcing the method names; couldn't solve the error  "both methods have same erasure"
    //
//    D toDomain(R resource);
//    D toDomain(E entity);
//    R toResource(D domain);
//    E toEntity(D domain);
}
